# DB Cabinet de Conseil：博客使用说明

已在原网站中加入独立博客区：

- 首页右下角新增 **Lire le blog →** 入口。
- 博客地址为 `/blog/`。
- 博客包含文章搜索框、系列文章区域、移动端适配。
- 当前示例文章链接到原有的 `/diagnostic-fournisseur-chine/` 页面。

## 上传到 Cloudflare Pages

1. 将本压缩包解压后，把全部文件上传到 GitHub 仓库的根目录。
2. 在 Cloudflare 进入 **Workers & Pages → Create application → Pages → Connect to Git**。
3. 选择该 GitHub 仓库，生产分支选择 `main`。
4. 这是已经构建好的静态网站，不需要 Jekyll 或 Node 构建：
   - Build command：留空
   - Build output directory：如果仓库根目录就是网站文件，留空或使用 `/`
5. 点击 **Save and Deploy**。

Cloudflare Pages 连接 Git 后，每次向生产分支推送文件，都会自动重新部署。

## 使用 Word 写博客

Word 文件不能直接变成博客文章页面。建议流程是：

1. 用 Word 写完文章并另存为 `.docx`。
2. 使用 Pandoc 转成 Markdown：

```bash
pandoc article.docx -t gfm -o article.md
```

3. 打开 `blog/blog.js`，在 `posts` 数组中新增一项，例如：

```js
{
  title: "我的新文章",
  date: "2026-10-01",
  series: "我的文章系列",
  order: 1,
  excerpt: "文章摘要。",
  tags: "商业 · 中国 · 法国",
  url: "articles/mon-article.html"
}
```

4. 将转换后的文章做成 HTML，放在 `blog/articles/`；或者直接把文章内容写入新的 HTML 页面。
5. 如果属于同一系列，保持 `series` 名称一致，并用 `order` 设置顺序。
6. 提交并推送到 GitHub：

```bash
git add .
git commit -m "Ajouter un nouvel article"
git push origin main
```

## 注意事项

- 现有首页是已构建的 React 静态文件，本次没有改动原有业务内容，只加入了一个独立入口，降低破坏风险。
- 后续如果你拿到原始 React 源码，建议把 Blog 入口放进源码导航，而不是继续直接修改构建后的 `index.html`。
- 如果网站部署在域名根目录，当前 `/blog/` 链接可直接使用；如果部署在某个子目录，需要把链接改成对应的子目录路径。
